<% 
    def flavor = "default" 
    def themeName = THEME.name ? THEME.name : "default";
%>
<link href="${ATTRS.contextPath}/themes/${themeName}/css/${flavor}/${OPTIONS.value}.css" type="text/css" rel="stylesheet" />
